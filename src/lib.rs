/// Adds one to the number given
///
/// # Examples
///
/// ```
/// let num = 5;
/// let sum = rust_learn::add_one(num);
///
/// assert_eq!(6, sum);
/// ```
pub fn add_one(x: i32) -> i32 {
    return x + 1;
}

// fn main() {}
